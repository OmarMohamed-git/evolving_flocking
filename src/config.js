'use strict';

const params = {
  preyCount: 200,
  predatorCount: 3,

  maxSpeed: 100,
  maxTurnForce: 200,

  perceptionRadius: 60,
  separationRadius: 25,

  alignmentWeight: 1.0,
  cohesionWeight: 0.9,
  separationWeight: 1.6,
};

const BOID_SIZE = 10;
